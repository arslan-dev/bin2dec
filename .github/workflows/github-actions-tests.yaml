name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@4
    - uses: actions/setup-node@v4
      with:
        node-version: 21.x
        cache: 'npm'
    - run: npm ci
    - run: npm test